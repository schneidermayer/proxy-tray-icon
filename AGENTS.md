# Repo Workflow

- After completing a user-requested code change, finish by committing the work, pushing the current branch, and running `./Scripts/build-app.sh` unless the user explicitly says not to.
